//api url
//set the ip of the local machine instead of local host, use ipconfig to get ip
const config = {
  // API_URL: "https://latshong.onrender.com",
  API_URL: "http://10.9.82.81:3000",
};
  
  export default config;
  