//api url
//set the ip of the local machine instead of local host, use ipconfig to get ip
const config = {
  // API_URL: "http://localhost:3000",
  API_URL: "http://192.168.71.50:3000",
};
  
  export default config;
  